sed -i 's/val id = repository.insertInvoice(invoice, invoiceItems)/val id = repository.insertInvoice(invoice, invoiceItems)\n            if (paidAmount > 0) {\n                addPayment(\n                    clientId = client.id,\n                    amount = paidAmount,\n                    paymentMethod = paymentMethod,\n                    notes = "دفعة مع فاتورة $invoiceNumber",\n                    invoiceId = id.toInt(),\n                    currency = currency\n                )\n            }/g' app/src/main/java/com/example/ui/viewmodel/AppViewModel.kt
